<Window x:Class="WpfApp1.View.OpenWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp1.View"
        mc:Ignorable="d"
        Title="OpenWindow" Width="1152" Height="546" WindowStartupLocation="CenterScreen">

    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
            <ColumnDefinition Width="*" />
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="2*" />
            <RowDefinition Height="2*" />
        </Grid.RowDefinitions>

        <StackPanel>
            <Button Margin="5" Name="OpenRepositroy" Click="OpenRepositroy_Click">
                <StackPanel>
                    <TextBlock> Open</TextBlock>
                </StackPanel>
            </Button>
            <Button Margin="5" Name="CreateRepository">
                <StackPanel>
                    <TextBlock> Create</TextBlock>
                </StackPanel>
            </Button>
            <Button Margin="5" Name="CloneRepository">
                <StackPanel>
                    <TextBlock> Clone</TextBlock>
                </StackPanel>
            </Button>

        </StackPanel>
    </Grid>
    
    
    
</Window>
